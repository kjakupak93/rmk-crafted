# Ship Skill
1. Update any relevant documentation files to reflect changes made this session
2. Run `git add -A`
3. Generate a descriptive commit message based on changes
4. Run `git commit` and `git push origin main`
4. Confirm deployment status
